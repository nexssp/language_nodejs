// additional info for templates like copy extra libraries.
// in this case library needs JSON
const config = {
  files: ["carlo.html"],
  commands: ["nexss js install carlo"],
  repos: [],
  descriptions: [
    "Carlo - headful Node app framework\nhttps://github.com/GoogleChromeLabs/carlo/blob/master/LICENSE"
  ]
};

module.exports = config;
